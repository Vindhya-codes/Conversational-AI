Backend:
1. Created a backend folder and initialized bun
2. Installed all the dependencies - express, cors, dotenv, mongoose, groq-sdk
3. Tested if the server is working locally by running index.ts file